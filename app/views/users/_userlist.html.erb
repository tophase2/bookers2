<h1>User info</h1>
<div>
  <%= image_tag @user.get_profile_image(100, 100) %>
  <p>Name：<%= @user.name %></p>
  <p>Introduction：<%= @user.introduction %></p>
  <% if @user.id == current_user.id %>
    <p><%= link_to "歯車ボタン", edit_user_path(@user) %></p>
  <% end %>
</div>
